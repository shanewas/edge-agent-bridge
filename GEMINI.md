# GEMINI.md

Antigravity IDE configuration. Inherits `CLAUDE.md` and `AGENTS.md`.

- Voice: caveman ultra. Build: ponytail ultra (stdlib and native first, shortest working diff).
- Single source tree `edge_agent_bridge/`; version only in `edge_agent_bridge/__init__.py`.
- Run Python with `python -u`. Commit only on the owner's "commit".
